import React from 'react';

const HeroSection = () => {
  return (
    <section className="hero">
      <h1>Urable: AI-Powered Development Orchestrator</h1>
      <p>Automate your development workflow with Urable. Read Jira tasks and generate GitHub pull requests without human intervention.</p>
      <div className="cta-buttons">
        <button className="primary">Try for Free</button>
        <button className="secondary">Watch Demo</button>
      </div>
    </section>
  );
};

export default HeroSection;